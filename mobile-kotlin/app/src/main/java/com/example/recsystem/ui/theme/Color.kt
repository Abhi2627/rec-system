package com.example.recsystem.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────

val CineRed        = Color(0xFFB2451E)   // primary CTA
val CineRedDark    = Color(0xFF8B3417)   // pressed / dark variant
val CineCream      = Color(0xFFF7E3C8)   // bottom nav, warm backgrounds
val CineNavy       = Color(0xFF1A1A2E)   // top headers
val CineGold       = Color(0xFFFFC107)   // star ratings

// ── Neutral scale ─────────────────────────────────────────────────────────────

val NeutralSurface = Color(0xFFF5F5F5)   // page background
val NeutralCard    = Color(0xFFFFFFFF)   // card surfaces
val NeutralBorder  = Color(0xFFE0E0E0)
val NeutralText1   = Color(0xFF1A1A1A)   // primary text
val NeutralText2   = Color(0xFF555555)   // secondary text
val NeutralText3   = Color(0xFF999999)   // hint / muted

// ── Semantic ──────────────────────────────────────────────────────────────────

val ErrorRed       = Color(0xFFD32F2F)
val SuccessGreen   = Color(0xFF2E7D32)
val TagPurple      = Color(0xFF6200EA)   // genre tags
